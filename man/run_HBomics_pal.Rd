% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HBomics.R
\name{run_HBomics_pal}
\alias{run_HBomics_pal}
\title{Main Wrapper to Run HBomics Pipeline}
\usage{
run_HBomics_pal(
  data_list,
  mutation_id,
  meta,
  bound = 0.95,
  chains = 4,
  warmup = 1000,
  iter = 2000,
  cores = 4,
  seed = 123,
  KL = FALSE,
  ...
)
}
\arguments{
\item{data_list}{a data list containing het. SNV informations:
T: total SNP number (int);
C: total subclone number (int);
S: total segment number (int);
segment_id: segment id, should be unique (array);
subclone_id: subclone id, should be unique (array);
n: SNV number of each segment (array);
A: alternative allele count of each SNV (array);
N: total allele count of each SNV (array);
b: genomic allele ratios of each SNV (array);
v_t: subclonal genomic variance (array);
v_j: segmental genomic variance (array);
lambda: Gamma scale paramter (numeric), default is 0.2;
mu0: mapping bias from enrichment data (numeric); 1 - mean(ref allele ratio);
sigma0: standard deviation of allele ratios from enrichment data (numeric)
c: smallest median SNV density for segments (int)
s: scale parameter in sigmoid function, default is 0.5}

\item{mutation_id}{vector containing unique mutation ID with the same order in data_list}

\item{meta}{user defined meta infro data frame;
first column should always be mutation_id;
this data frame can contains features (genes/regulatory elements per SNV resides) and other metrics
be sure it includes all mutations from the data_list and mutation_id}

\item{bound}{credit interval for the posterior distribution, defaul is 0.95}

\item{chains}{Number of Markov chains}

\item{warmup}{Number of warmup iterations}

\item{iter}{Total number of iterations}

\item{cores}{Number of CPU cores to use}

\item{seed}{Random seed}

\item{KL}{Logical flag if KL divergence would be conducted in the following computation}

\item{...}{Additional arguments passed to rstan::sampling}
}
\value{
An \code{HBomicsResult} object containing \code{hb_df},
\code{diagnostics}, and optionally \code{post_a} when \code{KL = TRUE}.
}
\description{
Main Wrapper to Run HBomics Pipeline
}
